# 点仔动效生成工具 · 使用指南（无编程基础版）

## 一、准备工作（只需一次）

### 1. 获取工具文件夹

从已安装的同学那里拷贝整个工具文件夹，里面应该包含：

```
点仔动效工具/
  ├─ 点仔动效工具-双击启动.command   ← 双击启动
  ├─ dianzai-motion-tool.html
  ├─ server.py
  └─ static/
       └─ examples/    ← 示例图片
```

把文件夹放到你电脑的任何位置（比如桌面）。

### 2. 第一次使用需要登录美境

**只有第一次使用时需要**，后续双击即可直接用。

**步骤：**
1. 打开「终端」App（按 `Command + 空格`，输入 `终端`，回车）
2. 粘贴下面的命令，回车：
   ```bash
   npm install -g @meigen/meigen-cli
   ```
3. 再粘贴下面的命令，回车：
   ```bash
   meigen login
   ```
4. 按提示操作，可能需要在大象 App 里点一下确认授权
5. 看到 "认证成功" 就搞定了

如果上面 `npm` 命令报错，先运行：
```bash
brew install node
```
（没有 brew 的同学去 https://nodejs.org 下载安装包双击安装）

---

## 二、日常使用（超简单）

### 双击启动

进入工具文件夹，**双击** `点仔动效工具-双击启动.command`

会自动完成：
- ✅ 检查环境
- ✅ 启动服务
- ✅ 打开浏览器

你什么都不用管，等浏览器打开直接用！

### 使用期间

- **不要关闭**弹出来的黑色终端窗口（服务在运行）
- 用完后直接关闭浏览器，再关闭终端窗口即可

---

## 三、常见问题

**Q: 双击 .command 文件提示"无法打开"？**
A: 右键点击文件 → 选择「打开」→ 点「仍要打开」

**Q: 提示"未找到 Python3"？**
A: macOS 自带 Python3，如果提示找不到，去「系统设置 → 软件更新」更新系统。

**Q: 提示"美境未登录"？**
A: 按上面「第一次使用」的步骤，在终端运行 `meigen login` 登录。

**Q: 点击生成后美境链接打不开？**
A: 美境任务绑定到提交者账号，每个人需要在自己的电脑上启动工具才能在自己的美境里看到任务进度。

**Q: 怎么分享给其他同事？**
A: 直接把整个工具文件夹复制给同事，同事按本指南操作即可。

---

## 四、备注

- 工具仅在 **macOS** 上测试通过
- 使用美境模型（Seedance）需要美团内网环境
- 所有 API Key 仅保存在浏览器页面，不会写入任何文件
